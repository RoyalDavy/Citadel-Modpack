asoiaf_jaimes_sword_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
	}
}

asoiaf_rhaegars_sword_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
	}
}

asoiaf_krakenfall_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
	}
}

asoiaf_red_priest_necklace_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
		OR = {
			has_trait = lifestyle_mystic
			has_trait = witch
		}
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
		#high_valyrian_opinion = 10 #change later to asshaii_opinion = 10
		dread_baseline_add = 5
	}

	unique = yes
	ai_score = {
		value = 100
	}
}

asoiaf_aerys_II_crown_template = {
	can_equip = {
		agot_can_wear_crown_trigger = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
		if = {
			limit = {
				NOR = {
					culture = {
						has_cultural_pillar = heritage_valyrian
					}
					religion = religion:the_rhllor_religion
				}
			}
			add = -10000
		}
		if = {
			limit = {
				any_equipped_character_artifact = {
					has_variable = stannis_crown_artifact
					NOT = { has_variable = asoiaf_aerys_II_crown_artefact }
				}
			}
			add = 100000 #In AGOT+'s modcanon, Stannis wears Aerys II's crown, not vanilla AGOT's crown
		}
	}
}

asoiaf_robert_I_crown_template = {
	can_equip = {
		agot_can_wear_crown_trigger = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
		if = {
			limit = {
				NOR = {
					dynasty ?= dynasty:dynn_Baratheon
					culture = culture:stormlander
				}
			}
			add = -10000
		}
		if = {
			limit = {
				any_equipped_character_artifact = { has_variable = robertI_crown_artifact }
			}
			add = 100000 #AI should always prefer AGOT+'s version of the crown if they have both available
		}
	}
}

asoiaf_stark_throne_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
}

asoiaf_radzig_sword_template = {
	can_equip = {
		always = yes
	}

	# can this character benefit from the full modifiers of the artifact?
	can_benefit = {
	}

	# if a given character does not pass the "can_benefit" trigger then this modifier will be applied instead.
	fallback = {
	}

	unique = yes
	ai_score = {
		value = 100
	}
}