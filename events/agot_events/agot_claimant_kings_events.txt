namespace = agot_claimant_king_events

agot_claimant_king_events.0001 = {
	hidden = yes

	immediate = {
		if = {
			limit = {
				NAND = {
					primary_title.tier < tier_empire
					any_character_war = {
						root = claimant
						is_attacker = root
						OR = {
							using_cb = claim_cb
							using_cb = agot_claim_cb
							using_cb = agot_succession_war
							using_cb = agot_claimant_faction_war
							using_cb = agot_bastard_claimant_war
							using_cb = agot_bastard_claimant_war_no_nickname
						}
						casus_belli = {
							any_target_title = {
								tier = tier_empire
							}
						}
					}
				}
			}
			remove_character_flag = agot_claimant_king
		}
	}
}